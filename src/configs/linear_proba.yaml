defaults:
  - model: ssl_model
  - writer: cometml
  - metrics: example
  - datasets: stl10
  - dataloader: example
  - transforms: stl10_no_augs
  - _self_
optimizer:
  _target_: torch.optim.AdamW
  lr: 1e-3
  weight_decay: 1e-4
lr_scheduler:
  _target_: torch.optim.lr_scheduler.StepLR
  gamma: 0.9
  step_size: ${trainer.epoch_len}
loss_function:
  _target_: src.loss.ExampleLoss
trainer:
  log_step: 50
  n_epochs: 20
  epoch_len: 500
  device_tensors: ["images", "labels"] # which tensors should be on device (ex. GPU)
  resume_from: null # null or path to the checkpoint dir with *.pth and config.yaml
  device: auto # device name or "auto"
  override: True # if True, will override the previous run with the same name
  monitor: "max test_Accuracy" # "off" or "max/min metric_name", i.e. our goal is to maximize/minimize metric
  save_period: 5 # checkpoint each save_period epochs in addition to the best epoch
  early_stop: ${trainer.n_epochs} # epochs for early stopping
  save_dir: "saved"
  from_pretrained: "saved/SimCLR-512 online-probe StepLR AdamW lr 1e-3 weight_decay 1e-4 epoch_len 500 n_epochs 10/checkpoint-epoch10.pth"
  using_head: true
  seed: 1
